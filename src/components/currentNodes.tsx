import React, {useEffect} from "react";

interface Props {};

const CurrentNodes: React.FC<Props> = ({}) => {
    useEffect(() => {}, []);
    return (
        <div className="current-nodes">
            Displaying the click-through graph for:
        </div>
    );
};

export default CurrentNodes;